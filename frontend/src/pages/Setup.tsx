/**
 * Setup page — shown on first run.
 *
 * Displays system hardware info and guides the admin through
 * installing Ollama and downloading the configured model.
 * Redirects to /admin once everything is ready.
 *
 * Full implementation in Task 8.
 */
export default function Setup() {
  return <div>Setup page — coming soon</div>
}
