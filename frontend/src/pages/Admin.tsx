/**
 * Admin configuration page.
 *
 * Allows the admin to pick the base model, set the context window size,
 * and select the UI language. Changes are persisted to the backend config.
 *
 * Full implementation in Task 9.
 */
export default function Admin() {
  return <div>Admin page — coming soon</div>
}
