"""
Setup domain package.

Mounted only in setup mode (``trove setup``). Handles the six-step setup
wizard (language, Ollama install, model pull, admin credentials, service
install) and the management dashboard endpoints.
"""
