"""
Config domain package.

Persists and loads the server-wide TroveConfig (model selection, context window
size, locale, admin credentials) as JSON at the XDG config path
~/.config/trove/config.json.
"""
