"""
Tasks domain package.

Defines the core Task/UserTask data models, Jinja2 prompt rendering,
the Pydantic AI task runner (stream and non-stream), and the Gems API
router. Tasks are stored in SQLite via the repository module.
"""
